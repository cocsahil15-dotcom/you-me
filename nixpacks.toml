[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3.11 -m pip install --break-system-packages flask gunicorn"]

[start]
cmd = "python3.11 -m gunicorn app:app --bind 0.0.0.0:$PORT"
